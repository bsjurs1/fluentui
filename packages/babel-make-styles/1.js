import { webLightTheme } from '@fluentui/react-theme';

const theme = createCSSVariablesProxy(webLightTheme);

function A() {}

import { makeStyles } from '@fluentui/react-make-styles';

makeStyles();
